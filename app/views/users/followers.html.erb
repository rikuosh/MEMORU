<h1>フォロワー一覧</h1>
<% @users.each do |user| %>
<div>
  <%= link_to user_path(user.id) do %>
  <%= attachment_image_tag user, :profile_image, size:"80x80" %>
  <% end %>
  <%= user.name %>
</div>
<% end %>